---
# =============================================================================
# KRNEL — requirements.yml
# Instala los roles y colecciones con: 
#  ansible-galaxy install -r requirements.yml
#  ansible-galaxy collection install -r requirements.yml
# =============================================================================

roles:
  - name: xanmanning.k3s
    version: "v3.6.2"

collections:
  - name: community.general
    version: ">=9.0.0"
  - name: kubernetes.core
    version: ">=5.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
